{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npx zenn preview",
            "type": "shell",
            "command": "npx zenn preview",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}